spring:
  data:
    redis:
      host: localhost
      port: 6379

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: "GOOGLE_ID"
            client-secret: "GOOGLE_SECRET"
            redirect-uri: "REDIRECT_URI"
            scope: profile, email
            authorization-grant-type: authorization_code